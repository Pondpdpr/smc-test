apiVersion: v1
kind: Service
metadata:
  name: {{ include "_TEMPLATE_-backend.fullname" . }}
  labels:
    {{- include "_TEMPLATE_-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "_TEMPLATE_-backend.selectorLabels" . | nindent 4 }}

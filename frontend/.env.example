# Backend's URL - calls go straight here rather than through Vite's dev/preview
# proxy. The backend's CORS_ORIGIN must include this frontend's origin(s)
# (see backend/.env.example) since this makes every request cross-origin.
# Change to the deployed backend's URL for a separate-host deployment.
VITE_API_URL=http://localhost:3000

# Used by `npm run dev` (Vite mode: development). Left empty - relative
# /v1/... calls are proxied to the backend by Vite's dev server (see
# vite.config.ts's server.proxy), so no CORS setup is needed for dev at all.
VITE_API_URL=http://localhost:3000
